using UnityEngine;

public class PlayerPrefsSaves
{
    public struct PlayerSavesKeys
    {
        public const string ScreenWidth = nameof(ScreenWidth);
        public const string ScreenHeight = nameof(ScreenHeight);
        public const string RefreshRate = nameof(RefreshRate);
        public const string GraphicsQuality = nameof(GraphicsQuality);
    }
}
